description: "Do a RANS simulation of turbulent flow through a square bend using simpleFoam with inlet velocity of 10 m/s, k-epsilon turbulence model, and a Newtonian fluid with kinematic viscosity of 1.5e-05 m^2/s. Control settings include: endTime = 500, and writeInterval = 50."
mesh_path: "Benchmark/mesh/Square_bend-laminar/blockMeshDict"

max_loop: 10
temperature: 0.7
run_times: 5
MetaGPT_PATH: "xxx"

# Qwen2.5-72B-Instruct
API_KEY: "xxx"
PROXY: "xxx"
BASE_URL: "xxx"
model: "xxx"
openfoam_llm_base_url: "xxx"
